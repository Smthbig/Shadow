<?xml version="1.0" encoding="utf-8"?>
<ScrollView
        xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="?attr/colorSurface">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Title -->
        <TextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Welcome to Shadow"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface" />

        <!-- Subtitle -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Control your app usage intentionally"
            android:textSize="14sp"
            android:layout_marginTop="8dp"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <!-- Divider spacing -->
        <View
            android:layout_width="match_parent"
            android:layout_height="24dp" />

        <!-- Privacy Policy Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Privacy Policy"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="?attr/colorOnSurface" />

        <!-- Privacy Content -->
        <TextView
            android:id="@+id/privacy_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Shadow does not collect or store personal data. All usage tracking is performed locally on your device using UsageStats APIs. No data is transmitted externally. The app only enforces limits at launch time and does not monitor background activity."
            android:textSize="14sp"
            android:layout_marginTop="12dp"
            android:textColor="?attr/colorOnSurfaceVariant" />

        <!-- Checkbox -->
        <CheckBox
            android:id="@+id/checkbox_accept"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="I agree to the Privacy Policy"
            android:layout_marginTop="24dp"
            android:textColor="?attr/colorOnSurface" />

        <!-- Continue Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/button_continue"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Continue"
            android:layout_marginTop="32dp"
            style="@style/Widget.Material3.Button" />

    </LinearLayout>
</ScrollView>
